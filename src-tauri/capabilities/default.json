{
  "$schema": "https://raw.githubusercontent.com/nicktomlin/tauri/tauri-v2/crates/tauri/schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default",
    "fs:allow-read",
    "fs:allow-write",
    "shell:default",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "scope": {
        "allow": [
          {
            "name": "ffmpeg",
            "cmd": "ffmpeg",
            "args": true
          },
          {
            "name": "ffmpeg.exe",
            "cmd": "ffmpeg.exe",
            "args": true
          }
        ]
      }
    }
  ]
}
